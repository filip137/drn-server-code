# AGENTS Override (Playbooks)

Use this override only for tasks described in `playbooks/optuna_analysis.md`.

Rules:
- Do not modify analysis blocks already present in `notes.md` unless explicitly asked.
- Keep analysis blocks between `AUTO_COST_ANALYSIS_START` and `AUTO_COST_ANALYSIS_END`.
- Use ASCII-only output and preserve existing formatting.

