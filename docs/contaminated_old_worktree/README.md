# Contaminated Old-Worktree Notes

These files preserve documentation copied from the old worktree after the WIP transfer.

They are intentionally archived here instead of merged into the canonical docs because the old worktree included simulations and interpretations affected by the `adaptive_equilibrium=True` problem. Treat numerical results, gradient conclusions, run-selection decisions, and "best row" claims in this directory as contaminated unless they are independently revalidated with the corrected fixed-equilibrium protocol.

Use these notes only as historical context and as a checklist of hypotheses or runs to recheck.

Source snapshot: commit `5e2d76fc` (`WIP park amplification paper work from main worktree`).
