# Out-of-Scope Documentation Archive — 2026-07-29

These files were moved out of the active documentation path when the current
Conv scope was narrowed to perfect-diode BPTT Conv1/Conv2/Conv3 work.

They remain provenance. They are not authority for new runs. The scientific
body of each moved file is unchanged; Git history records its former path.

The archive contains:

- hard-sigmoid input-gain, `T/K`, learning-rate, optimizer, and
  finite-conductance diagnostics;
- Hopfield/EqProp and stale scheduler-status documents;
- Optuna and ReLU-to-DRN notes outside the active Conv question; and
- legacy dense-amplification sweep READMEs formerly stored under
  `experiments/`.

Use the active [Conv protocol index](../../conv_paper_hyperparameter_protocol.md)
for new work.
