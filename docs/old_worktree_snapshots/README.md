# Old-Worktree Snapshots

These files preserve old-worktree notes that are not considered contaminated by the `adaptive_equilibrium=True` issue.

They are kept separate from `docs/contaminated_old_worktree/` so the affected BP/DRN simulation notes remain clearly separated from unaffected experiment records.

Source snapshot: commit `5e2d76fc` (`WIP park amplification paper work from main worktree`).
