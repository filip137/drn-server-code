# Conv3 p95 read-noise 1e-5 and 1e-4

Filip explicitly confirmed sigma1e-5 and1e-4 on September21,2026 and requested stopping the entire group if any run fails. Six fresh seed0 cases: baseline/ours/legacy at each level;30epochs each. The stop-all group also includes the three active5e-4 cases. The previously cancelled1e-3 jobs remain excluded. No numerical retries or additional noise levels.

All scientific fields inherit exactly from the5e-4 plan except sigma and descriptive IDs. Injected beta baseline147.682614594, ours2.49274796756, legacy2.81845428732; per-matrix strict cosine>.95 selection, fixed T=K=8, input gain360, float64 centered frozen-current EqProp, saved initializer, fresh Adam and exact inherited learning rates, zero frozen biases, bounded weights[0,100]. MNIST55k/5k, no official test. Noise seed2026081601; noise only on noninput endpoint readouts. Reuse the accepted T/K authority and finite-T caveat. Frozen scientific archive74f41a03ce4718eebcd227bb38e4bbd2309e6b30bb9dd5515c099bb880068819.

Execution: Jean Zay fmu@v100, gpu_p13, qos_gpu-t3, V100-16GB,4CPUs/1GPU per task; array0-5%3, one six-hour allocation per case. A short outer shell runs the unchanged gated ten-epoch continuation worker three times automatically. No manual scientific review at epochs10 or20. At most three additional GPUs, hence at most six alongside5e-4. Expected10–12hours for two waves plus queue time; production cap36GPUh,37GPUh including gates. Hard deadline2026-09-23T10:30:09.440114Z.

Gate: local real-runner smoke for all six configs, shell/simulated outer-wrapper tests, scheduler test-only, one live singleton V100 canary under the new QoS through all six cases and both restores. Shared stop-all observer is tested separately and includes all submitted production arrays; any training/scheduler failure or a completed run failing the existing final-drop screen triggers cancellation of all queued/running group jobs. Observability errors stop and report; they do not imply a scientific failure. No subsequent submissions after a stop trigger.

Final review:30finiteepochs and final validation strictly less than5pp below own best; report curves, losses, temporary drawdown and failure location. Single-seed diagnostic. Matched clean controls remain separate and unavailable until completed. Collect terminal snapshots locally and register before final interpretation.
